packages:
  # Core dbt utility macros (surrogate keys, pivot, star, etc.)
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]

  # Before/after model comparison — invaluable for refactors
  - package: dbt-labs/audit_helper
    version: [">=0.9.0", "<1.0.0"]

  # Extended test suite: expect_column_values_to_be_between, quantile tests, etc.
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<1.0.0"]
